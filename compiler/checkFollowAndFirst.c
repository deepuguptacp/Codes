#include<stdio.h> 
#include<ctype.h> 
#include<string.h> 
char production[10][100];
int main(){
    char str[100]="";
    scanf("%s", str);
    strcpy(production, "str");
    printf("%s", production);
    return 0;
}